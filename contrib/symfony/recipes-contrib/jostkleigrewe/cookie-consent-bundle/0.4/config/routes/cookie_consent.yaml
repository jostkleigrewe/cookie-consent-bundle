# DE: Lädt die #[Route]-Attribute aus den Controllern des Bundles.
# EN: Loads #[Route] attributes from the bundle's controllers.
cookie_consent:
    resource:
        path: '@CookieConsentBundle/Controller/'
        namespace: Jostkleigrewe\CookieConsentBundle\Controller
    type: attribute
